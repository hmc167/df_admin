import 'package:flutter/material.dart';
import 'package:get/get.dart';

class StateDistrictView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('StateDistrict Screen')),
    );
  }
}
